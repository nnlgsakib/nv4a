/** @module semantic-release-gh-pages-plugin */
import * as dot from 'dot';
/**
 * @private
 */
export const render = (template, context, logger) => {
    try {
        return dot.template(template)(context);
    }
    catch (err) {
        logger.error('dot-template render failure', err);
        return template;
    }
};
